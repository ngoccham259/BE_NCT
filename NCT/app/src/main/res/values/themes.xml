<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.NCT" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->

        <item name="colorPrimary">@color/colorAdminGreen</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="colorAccent">@color/colorAccent</item>

        <!-- THÊM DÒNG NÀY để Toolbar nhận diện style search -->
        <item name="searchViewStyle">@style/CustomSearchViewStyle</item>


    </style>

    <style name="Theme.NCT" parent="Base.Theme.NCT" />
</resources>